<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Cold-start / SplashScreen theme: dark background + app logo -->
    <style name="Theme.IkhsanaHotelTV.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.IkhsanaHotelTV</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>

    <!-- Post-splash app theme: black window avoids white flash on cold launch -->
    <style name="Theme.IkhsanaHotelTV" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <!-- Disable platform square focus highlight on Android TV (API 26+) -->
        <item name="android:defaultFocusHighlightEnabled">false</item>
        <item name="android:colorControlHighlight">@android:color/transparent</item>
        <item name="colorControlHighlight">@android:color/transparent</item>
        <item name="android:selectableItemBackground">@drawable/focus_transparent</item>
        <!-- Allow adjustResize to shrink content above the IME -->
        <item name="android:windowFullscreen">false</item>
    </style>
</resources>
