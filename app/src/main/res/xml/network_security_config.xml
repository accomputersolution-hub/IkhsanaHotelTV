<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows HTTPS to public CAs (ngrok / Headscale) and cleartext for LAN debug.
  Trusts system + user-installed CAs so corporate / test CAs work with Java
  networking; libtailscale also receives user PEMs via getUserCACertsPEM().
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">ngrok-free.dev</domain>
        <domain includeSubdomains="true">ngrok.io</domain>
        <domain includeSubdomains="true">lhr.life</domain>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </domain-config>
</network-security-config>
