# Place a full WireGuard config here for corporate kiosk VPN (no UI).
# Example (replace keys / endpoint before production use):
#
# [Interface]
# PrivateKey = <device_private_key>
# Address = 10.10.0.2/32
# DNS = 1.1.1.1
#
# [Peer]
# PublicKey = <server_public_key>
# AllowedIPs = 0.0.0.0/0, ::/0
# Endpoint = vpn.example.com:51820
# PersistentKeepalive = 25
#
# Until a real config is provided, the internal VPN stays idle and logs clearly.
