package `in`.pcncloud.hotel.ui.home

import `in`.pcncloud.hotel.R

/** Corporate flavor — local splash / fallback brand mark (never network). */
internal object BrandAssets {
    val logoRes = R.drawable.lt_logo
}
